1. Download and install nodejs
https://nodejs.org/en/download/prebuilt-installer

2. Check node and npm version
cmd:>node -v 
cmd:>npm -v 

3. Created Node folder and also create session1 folder inside node folder

4. create index.js file inside session1 directory

5. to run node 
cmd:>node index.js

6. create package.json 
cmd:>npm init or npm init -y

7. install nodemon 
cmd:>npm i nodemon 

8. create script to run app in package.json 

"scripts":{
    "start":"nodemon index.js"
}


1. Install babel
cmd:> npm i @babel/node @babel/cli @babel/core @babel/preset-env 

2. update package.json

  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  }

  also, update scripts in package.json

    "scripts":{
      "start": "nodemon --exec babel-node index.js" 
    }


authorization
authontication
404 router issue
encription


singnup ke time (name email pass contact) =req.body

exist user= check karenge email exits hoga data aayega   =await()
exist hoga toh pass hoga if me return karega err user exist
pass word incription
bcrypt   npm i  bcrypt 

series
round 9 pasword hasing -48hes/sec

hasing  password encryption type 


(plan pass,series)
md - 5 genrate random password

save password in userpassword


singnup - 
//login

sing-up - user cretae nai hoga 
email pass
!exist =user out ho jayega  messga user does not exit
exist karega toh =pass hoga
check karega password fir wo check karega "compare se passsword correct hai ya nai through hasing se

400 error
!match return "invalid credential"
if matches all =  return res.status(200).json

don want show pass
userdata =await
select('-password')

envoirment   (system envioyrment variable )create global variable
nodeenvioment crete karta
.env = nedd globally use 
#db   DB_URL = KUCH BHI CHNAGE KANA HA TOH  
use capital variable

run time enviormnert
intall dotenv


.files = single date multi data   dono lega  in frontend

///
smtp
node mailler package  

pasowrd reset




 react =cross origine 3000 series
node = 8000
do port se request aayega toh cross orign ka error aayega
view folder me sikhaya hai

to handdle the cross origin error

npm i cross
import in index

app.use(cors())

dont want send data to facebook
3001 se aaye toh nai dena hai

corsoption   = stopped to send the date on diffrent port 

payment getway

pasowrd change 
password forgott


email
pass 
new
if correect the update if wrong redirect error through

//authorisation

//auhtentication user validation check karta hai

jwt token  
pakeg jwt install
authontication=authorization
//test 
//get public
//admin,product,order == add , all users check , delete

task validation
login or signup ke url ke bith me lagega befor callback
express validator

//get users have admin access

//get user is globally access by user and admin

//local-host delete na ho 
api hit per tokencrete hoga |

 ** add to cart functionallity
 cart.js in controller
1 getcart
2 additem
3 removeitem 

 model cart.model

 cart router



  

